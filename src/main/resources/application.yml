spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finalQuiz?useUnicode=true&character_set_server=utf8mb4
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
